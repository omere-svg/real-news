{
  "name": "project-horizon",
  "version": "0.1.0",
  "description": "Autonomous intelligence agent — server-side executive editor (Phase 1)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test scripts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node --env-file-if-exists=.env dist/main.js",
    "start:dev": "node --env-file-if-exists=.env --import tsx src/main.ts",
    "verify:bot": "node --env-file-if-exists=.env --import tsx scripts/verify-bot.ts",
    "backfill:summaries": "node --env-file-if-exists=.env --import tsx scripts/backfill-summaries.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "@libsql/client": "^0.14.0",
    "drizzle-orm": "^0.39.0",
    "fast-xml-parser": "^4.5.0",
    "hono": "^4.6.0",
    "openai": "^4.77.0",
    "yaml": "^2.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^3.2.7",
    "drizzle-kit": "^0.30.0",
    "eslint": "^10.6.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.63.0",
    "vitest": "^3.0.0"
  }
}
